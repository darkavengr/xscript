FUNCTION [function name] { AS [type] }
.
.
.
ENDFUNCTION

Declare a function.

A function may be declare with an optional type that may be SINGLE, DOUBLE, INTEGER, STRING or a user-defined type.

Without the optional type, function declarations uses the single-precision data type.

See also: SINGLE DOUBLE INTEGER STRING TYPE ENDTYPE

